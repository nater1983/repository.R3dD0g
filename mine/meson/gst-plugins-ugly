mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Ddoc=disabled \
    -Dnls=disabled \
    -Dtests=disabled \
    -Dgpl=enabled
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
