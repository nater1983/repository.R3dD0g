mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dlogind=none \
    -Dgsd_plugin=true \
    -Dman=true
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
