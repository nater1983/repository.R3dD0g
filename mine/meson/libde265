mkdir cmake-build
cd cmake-build
  cmake .. \
    "${GNOME_CMAKE_ARGS[@]}" \
    "${GNOME_CMAKE_OPT_ARGS[@]}" \
    -DENABLE_SHERLOCK265=OFF
make -C ../cmake-build
make -C ../cmake-build install DESTDIR=$PKG
