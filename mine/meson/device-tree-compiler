rm -f Makefile

mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dtests=false \
    -Dvalgrind=disabled
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
