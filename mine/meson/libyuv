if [ "$ARCH" = "x86_64" ]; then
    sed -i 's+DESTINATION lib+DESTINATION lib64+g' CMakeLists.txt
fi

mkdir cmake-build
cd cmake-build
  cmake .. \
    "${GNOME_CMAKE_ARGS[@]}"
make -C ../cmake-build
make -C ../cmake-build install DESTDIR=$PKG
