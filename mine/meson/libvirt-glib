mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Ddocs=enabled \
    -Dtests=disabled \
    -Dintrospection=enabled \
    -Dvapi=enabled
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
