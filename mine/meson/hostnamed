mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${MINE_BUILD_ARGS[@]}" \
    $MINE_OPT_ARGS \
    -Ddbus_system_conf_dir=/usr/share
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
