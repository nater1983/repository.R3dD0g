mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dintrospection=true \
    -Dtests=false \
    -Dgtkdoc=false \
    -Dstemmer=false \
    -Dcli=true
  "${NINJA:=ninja}"
install -vDm 0644 src/Xmlb-2.0.gir -t $PKG/usr/share/gir-1.0
install -vDm 0644 src/Xmlb-2.0.typelib -t $PKG/usr/lib64/girepository-1.0
