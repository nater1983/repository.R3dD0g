rm -fr po
git clone https://github.com/dialect-app/po.git

mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
