# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

       |-----handy-ruler------------------------------------------------------|
foundry: foundry (Kibrary and companion command-line tool)
foundry:
foundry: This tool aims to extract much of what makes GNOME Builder an IDE
foundry: into a library and companion command-line tool. The foundry library
foundry: is built heavily upon libdex which is a library I wrote based on more
foundry: than 2 decades of writing both concurrent and parallel programs and
foundry: libraries.
foundry:
foundry: Homepage: https://gitlab.gnome.org/chergert/foundry
foundry:
foundry:
