patch -Np1 -i $CWD/patch/tpm2-tss/1.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
# Do not use <openssl/engine.h> (fixed upstream for 4.2)
patch -Np1 -i $CWD/patch/tpm2-tss/tpm2-tss-4.1.3-openssl-no-engine.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
