patch -Np1 -i $CWD/patch/libyuv/libyuv-0002-Use-a-proper-so-version.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/libyuv/libyuv-0003-Link-against-shared-library.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
