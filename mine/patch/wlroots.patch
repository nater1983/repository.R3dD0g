patch -Np1 -i $CWD/patch/wlroots/0001-Revert-layer-shell-error-on-0-dimension-without-anch.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/wlroots/0001-xwm-Handle-NET_WM_WINDOW_OPACITY.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/wlroots/0001-seat-Don-t-forget-to-destroy-touch-points-on-touch-u.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
