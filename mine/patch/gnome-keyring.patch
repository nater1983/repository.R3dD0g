patch -Np1 -i $CWD/patch/gnome-keyring/001-pam-manpage.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/gnome-keyring/pam-libdir.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
