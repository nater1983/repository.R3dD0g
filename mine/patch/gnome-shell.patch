#patch -Np1 -i $CWD/patch/gnome-shell/3939-rediffed.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
#patch -Np1 -i $CWD/patch/gnome-shell/0001-status-keyboard-Add-a-catch-around-reload-call.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
#patch -Np1 -i $CWD/patch/gnome-shell/0002-status-keyboard-Load-keyboard-from-system-settings-i.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
#patch -Np1 -i $CWD/patch/gnome-shell/0003-status-keyboard-Use-gnome-desktop-API-for-getting-de.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/gnome-shell/add-login-background-support.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/gnome-shell/1747a59aa5d44f4d7b74e1f1fee1d48174a1ce5a.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
#patch -Np1 -i $CWD/patch/gnome-shell/3801.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
