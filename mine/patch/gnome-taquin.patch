patch -Np1 -i $CWD/patch/gnome-taquin/0001-Don-t-alter-or-try-to-write-GtkChild-fields.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/gnome-taquin/0001-Reference-of-GtkChild-fields-is-handled-by-GtkBuilde.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
