patch -Np1 -i $CWD/patch/mutter/0001-meson-Update-the-syprof-wrap-to-47.alpha-and-sysprof.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/mutter/0001-gschema-Enable-fractional-scaling-experimental-featu.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
patch -Np1 -i $CWD/patch/mutter/mutter-48.0-upstream_fixes-1.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
