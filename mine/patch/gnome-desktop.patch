#patch -Np1 -i $CWD/patch/gnome-desktop/0002-gnome-languages-Add-function-to-detect-non-latin-lay.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
#patch -Np1 -i $CWD/patch/gnome-desktop/0003-meson-Add-codegen-for-localed.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
#patch -Np1 -i $CWD/patch/gnome-desktop/0004-languages-Add-functions-for-getting-default-input-so.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
