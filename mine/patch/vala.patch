patch -Np1 -i $CWD/patch/vala/37813b8747dc9633f8cccba4a5a8b86955184789.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
# Don't use graphviz if it's not on the system.
if ! /bin/ls /usr/man/man7/graphviz.7.gz 1> /dev/null 2> /dev/null ; then
  patch -Np1 -i $CWD/patch/vala/disable-graphviz.patch || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
  # The last one doesn't quite do it for 0.42.3.
  patch -Np1 -i $CWD/patch/vala/no.gvc-compat.c.diff || { touch ${SLACK_GNOME_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
  GRAPHVIZ_SUPPORT="--disable-graphviz"
  autoreconf -vif || exit 1
fi
