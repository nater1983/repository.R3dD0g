mkdir -p $PKG/usr/bin
ln -sf /opt/$PKGNAME/Telegram $PKG/usr/bin

mkdir -p $PKG/usr/share/applications
cat $CWD/post-install/telegram/$PKGNAME.desktop > $PKG/usr/share/applications/$PKGNAME.desktop

mkdir -p $PKG/usr/share/pixmaps
cp $CWD/post-install/telegram/$PKGNAME.png $PKG/usr/share/pixmaps
